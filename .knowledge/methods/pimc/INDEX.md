# pimc — references

Generated 2026-06-26. Method card KB for path-integral Monte Carlo.

Search this KB from its parent with `rg --hidden -g '!.raw' "term" pimc/`, or run `rg --hidden -g '!.raw' "term" .` from inside the KB. The `.raw/` subdir holds the original PDFs / clones / HTML and is gitignored.

