## Summer plan

1. autocorrelation of the order-parameter operator C(τ)=⟨O(τ)O(0)⟩∼τ^{−λ}, not C(τ)∼e^{−τ/ξτ}: fit C(τ) to both exponential and power-law or stretched forms. If a single exponential fails badly while a slow algebraic form works over a broad intermediate window, that is strong evidence. (Q: ordering at low T? order parameter?) 
2. Measure the low-temperature specific heat, Cv(T)∼T^λ: show compatible non-universal low-temperature power laws over the same parameter window. (Q: tuning parameter?)
3. Average–typical separation in bulk observables: Xtyp=exp lnXavg: average is not representative of a typical disorder realization, if the average susceptibility diverges or grows strongly while the typical susceptibility stays much smaller, that is a strong Griffiths-compatible signal, similarly, the correlation function in space or time. (Q: without disorder, average over what?)
4. distribution of gaps: compare the mean gap and typical gap Δtyp≪⟨Δ⟩ , Δtyp=exp⟨lnΔ⟩. 
5. negative checks: not a conventional disordered paramagnet because C(τ) and χ(T) are not simply exponential; not just a critical point because the anomalous behavior persists over a finite parameter window. 
*.  What are the quasi-good quantum numbers in the current case == the order parameter in the rare region in the strong disorder spin model?  => local not in real space but in phase space?